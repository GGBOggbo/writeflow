/**
 * Search-scoped logger — thin wrapper around the unified `log`.
 *
 * Import from here if you only need search-specific logging.
 * For cross-cutting logging, import from `@/lib/debug` directly.
 */

export { log } from "@/lib/debug";
